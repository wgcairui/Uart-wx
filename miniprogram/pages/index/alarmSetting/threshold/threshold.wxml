<!--miniprogram/pages/index/alarmSetting/threshold/threshold.wxml-->
<page-meta>
  <navigation-bar front-color="#ffffff" background-color="#487ed9" />
</page-meta>
<view class="page">
  <!-- 顶部 Hero -->
  <view class="hero">
    <view class="hero-bg"></view>
    <view class="hero-content">
      <view class="hero-label">参数限值</view>
      <view class="hero-title">{{name}}</view>
    </view>
  </view>

  <view class="content">
    <view class="param-card">
      <view class="param-row">
        <view class="param-row-label">最小值</view>
        <view class="param-row-value">
          <view class="stepper">
            <view class="stepper-btn" bindtap="onMinMinus">−</view>
            <text class="stepper-num">{{min}}</text>
            <view class="stepper-btn" bindtap="onMinPlus">+</view>
          </view>
        </view>
      </view>
      <view class="param-divider"></view>
      <view class="param-row">
        <view class="param-row-label">最大值</view>
        <view class="param-row-value">
          <view class="stepper">
            <view class="stepper-btn" bindtap="onMaxMinus">−</view>
            <text class="stepper-num">{{max}}</text>
            <view class="stepper-btn" bindtap="onMaxPlus">+</view>
          </view>
        </view>
      </view>
      <view wx:if="{{unit}}" class="param-foot">
        <text>单位：{{unit}}</text>
      </view>
    </view>

    <view class="hint">
      <text>提示：最大值不能小于最小值。点击底部"确认"保存修改并返回。</text>
    </view>
  </view>

  <view class="save-bar">
    <view class="save-bar-btn" bindtap="onSubmit">确认修改</view>
  </view>
</view>
